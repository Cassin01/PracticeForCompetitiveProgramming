#include <stdio.h>

struct Price {
  int hamburger;
  int potato;
  int drink;
};

struct Order {
  int numer;
  int hamburger;
  int potato;
  int total;
};

struct Price Set_Price(struct Price p);
void Cal_Total(struct Price, struct Order*);
void Disp_Total(struct Order);
void Set_Hamburger(int);
void Set_Potato(int);
void Set_Drink(int);


struct Price Set_Price(struct Price p) {
  p.hamburger = 200;
  p.potato    = 180;
  p.drink     = 150;
  return p;
}

void Cal_Total(struct Price p, sturct Order *o) {
  
}
