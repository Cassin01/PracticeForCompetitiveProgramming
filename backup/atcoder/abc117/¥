#include <iostream>
#include <vector>
using namespace std;

int main(void) {
  int N, K;
  int tp;
  vector<int> v;
  cin >> N;
  cin >> K;
  for(int i = 0; i < N; i++) {
    cin >> tp;
    v.push_back(tp);
  }
  int max_ = 0;
  //for(int i = 0; i <= K; i++) {
    int f = 0;
    int d = 4;
    for(int j = 0; j < N; j++) {
      cout << d ^ v[j];
    }
    if (max_ < f) {
      max_ = f;
    }
  //}
  cout << max_ << endl;
  return 0;
}

vector<int> t_t(int decimal) {
  vector<int> t;
  while(decimal > 0) {
    
  }
  return t;
}
